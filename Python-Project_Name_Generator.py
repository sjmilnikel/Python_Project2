#!/usr/bin/env python3.7

import random

# EC2 instances - names, number, department

# How many EC2s they want a name for
# Dept name
# Generator random names and characters

unique_EC2s = []

int(input("How many EC2 instances do you want names for?" ))
print(unique EC2 names)

input("What is the name of your department?" )


randon.randint()
random.randrange()
random.choice()   ??
random.choices()   ??


def gen_range(stop, start=1, step=1):
    num = start
    while num <= stop:
        yield num
        num += step
    
    for num in gen_range( )